import type { RuntimeStateTarget } from './files/file-platform.js';
import {
  resolveRuntimeStateTarget,
  writeAtomically,
} from './files/file-platform.js';

export type { RuntimeStateTarget };
export { resolveRuntimeStateTarget, writeAtomically };
